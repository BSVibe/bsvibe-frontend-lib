/**
 * @bsvibe/layout — placeholder.
 *
 * Phase A 후속에서 본 구현:
 *  - <AppShell> (RSC: header + sidebar + main slot)
 *  - <ResponsiveSidebar> (Client: collapsible, swipe-to-close, Next.js Link/usePathname)
 *  - <ResponsiveTable> (Phase B 모바일 가로 스크롤 + sticky 첫 컬럼)
 *
 * SoT: 4개 제품의 ~80 LOC 동일 보일러플레이트 (BSGateway/BSNexus가 가장 풍부).
 */

export const _placeholder = "@bsvibe/layout" as const;
