/**
 * @bsvibe/design-tokens — placeholder.
 *
 * Phase A 후속에서 본 구현:
 *  - Tailwind 4 preset (color/spacing/typography)
 *  - CSS variables (stitch-* Material 3 palette)
 *  - BSGateway 하드코딩 색상 → CSS 변수 전환 (D4)
 *  - SoT: BSNexus `design-tokens.ts` (Phase Z 후속 추출)
 *
 * See bsvibe-site, BSNexus for current token usage.
 */

export const _placeholder = "@bsvibe/design-tokens" as const;
